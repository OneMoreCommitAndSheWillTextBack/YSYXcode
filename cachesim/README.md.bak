# CacheSim - Cache 功能模拟器

## 项目简介

CacheSim 是一个轻量级的 cache 功能模拟器，用于在 cache 设计空间探索（Design Space Exploration, DSE）过程中快速统计 cache 缺失次数，从而估算总缺失时间（Total Miss Time, TMT），以评估不同 cache 参数配置对 IPC 的影响趋势。

本项目采用现代 C++17 实现，旨在学习和实践现代 C++ 的最佳实践和特性。

## 背景与动机

### 设计空间探索的挑战

在给定的资源约束下，选择合适的 cache 参数（如容量、关联度、块大小等）以达到较好的性能表现，属于 cache 的设计空间探索问题。我们关心的性能指标包括：

- **IPC** (Instructions Per Cycle)
- **主频** (可通过 yosys-sta 项目快速评估)
- **面积** (可通过 yosys-sta 项目快速评估)

对于 IPC，通常需要在校准过访存延迟的 ysyxSoC 环境中运行完整程序后才能获得。但不同参数之间的组合情况太多，如果针对每一种参数组合都要花费小时级别的时间才能得到其 IPC，设计空间探索的效率将会十分低下。

### 解决方案：TMT 作为 IPC 的代理指标

由于数据精度和仿真效率是权衡关系，为了提升设计空间探索的效率，我们可以牺牲 IPC 的统计精度，通过较低的开销来统计一个能体现 IPC 变化趋势的指标。

当我们调整 cache 的各种参数时，直接影响的是 **AMAT** (Average Memory Access Time)。根据 AMAT 的定义，调整上述参数并不影响 cache 的访问时间（可看作常数），因此我们真正需要关注的，只有程序在发生 cache 缺失时需要等待的总时间，我们将其称为 **总缺失时间（Total Miss Time, TMT）**。

**TMT 可以代表 IPC 的变化趋势**：TMT 越小，每条指令访存所需的周期数也越小，从而 IPC 越大。

## 设计思路

### 缺失次数的统计

为了低开销地统计 TMT，我们从"缺失次数 × 缺失代价"这个角度考虑：

1. **指令流是固定的**：对于给定的程序，cache 的访问次数是固定的。只要获得程序运行的 itrace，将其输入到 icache 中，就可以模拟 icache 工作的过程，从而统计 icache 的缺失次数。

2. **不需要完整仿真**：NPC 执行程序时，需要通过当前指令的执行结果来得知下一条需要执行的指令。但 itrace 已经包含了完整的指令流，因此在统计 TMT 时，我们只需要指令流的 PC 值，而不需要指令本身。

3. **只需要元数据**：icache 的数据部分是用来作为指令回复给 NPC 的 IFU 的，但由于统计 TMT 并不需要 NPC，因此 icache 的数据部分也不需要，只需要保留元数据部分即可。对于给定的访存地址序列，cache 的缺失次数与访存内容无关，通过维护元数据即可统计出正确的缺失次数。

因此，为了统计 icache 的缺失次数，我们完全没有必要每次都把程序完整运行一遍。我们真正需要的是一个简单的 cache 功能模拟器——**CacheSim**。

### 缺失代价的估算

由于 CacheSim 不包含 ysyxSoC 中的访存细节，因此原则上我们也无法准确地获取缺失代价。但根据讨论，只有块大小这个参数会影响缺失代价，因此我们可以在 ysyxSoC 中统计出一个平均缺失代价，并将其作为一个常数来估算 TMT。

## 工作流程

1. **生成指令流**：通过 NEMU 快速生成指令流的 PC 序列（简化版的 itrace）
2. **模拟 cache 行为**：CacheSim 接收 PC 序列，通过维护 cache 元数据来统计缺失次数
3. **计算 TMT**：TMT = 缺失次数 × 平均缺失代价（常数）
4. **评估性能趋势**：TMT 越小，IPC 越大

## 项目特性

- ✅ **高效**：不需要运行完整的 ysyxSoC 仿真，只需要处理指令流
- ✅ **轻量**：只需要维护 cache 元数据，不需要实际的数据存储
- ✅ **快速**：可以快速评估大量参数组合的性能趋势
- ✅ **准确**：对于缺失次数的统计是准确的，缺失代价可以通过校准获得
- ✅ **灵活**：支持 icache 和 dcache 两种模式
- ✅ **易用**：使用配置文件系统，支持简单的配置管理

## 项目架构

### 目录结构

```
cachesim/
├── include/                    # 头文件
│   ├── Cache.h                # Cache 主类
│   ├── CacheConfig.h          # Cache 配置结构
│   ├── Itraceparser.h         # 指令流解析器
│   └── Mtraceparser.h         # 数据访存流解析器
├── src/                       # 源文件
│   ├── Cache.cpp              # Cache 实现
│   ├── main.cpp               # 主程序入口
│   ├── Itraceparser.cpp       # 指令流解析器实现
│   └── Mtraceparser.cpp       # 数据访存流解析器实现
├── cfglang/                   # 配置解析库（通用库）
│   ├── include/
│   │   ├── Cfgparser.h        # 配置解析器头文件
│   │   └── Cfgparser_impl.h   # 模板实现
│   └── src/
│       └── Cfgparser.cpp      # 配置解析器实现
├── CMakeLists.txt             # CMake 构建文件
├── Makefile                   # Makefile 构建（可选）
├── cfg.txt                    # 配置文件示例
├── itrace-log.txt             # 示例指令流文件
├── mtrace_log.txt             # 示例数据访存流文件
└── README                     # 本文档
```

### 核心模块

#### 1. Cache 类

实现了完整的 cache 模拟器，支持：

- **Cache 类型**：
  - 直接映射（Direct Mapped）
  - 全相联（Fully Associative）
  - 组相联（Set Associative）

- **替换策略**：
  - LRU（Least Recently Used）
  - FIFO（First In First Out）
  - Random（随机替换）

- **功能**：
  - `access()`: 模拟 cache 访问
  - `reset()`: 模拟 cache 行失效（用于 dcache 写操作）
  - `get_hit_time()`: 获取命中次数
  - `get_miss_time()`: 获取缺失次数
  - `get_miss_rate()`: 获取缺失率

#### 2. 配置文件系统（cfglang）

通用的配置解析库，支持：

- 基于块的配置格式（使用 `[block_name]` 标识）
- 类型安全的配置值获取
- 模板化的配置块类型系统

示例配置文件 `cfg.txt`：
```ini
[dcache]
offset-len = 4
index-len = 3
log = /path/to/mtrace_log.txt

[icache]
offset-len = 4
index-len = 3
log = /path/to/itrace-log.txt
```

#### 3. Trace 解析器

- **ItraceParser**: 解析指令流文件（itrace）
- **MtraceParser**: 解析数据访存流文件（mtrace）

## 使用方法

### 编译

使用 CMake 构建：

```bash
mkdir build && cd build
cmake ..
make
```

或者使用 Makefile：

```bash
make
```

### 运行

#### 运行 icache 模拟

```bash
./bin/cachesim icache [config_file]
```

示例：
```bash
./bin/cachesim icache ./cfg.txt
```

#### 运行 dcache 模拟

```bash
./bin/cachesim dcache [config_file]
```

示例：
```bash
./bin/cachesim dcache ./cfg.txt
```

### 配置文件格式

配置文件使用 INI 风格的格式，支持 `[block_name]` 块标识：

```ini
[icache]
offset-len = 4      # block offset 位数
index-len = 3       # set index 位数
log = itrace-log.txt # 指令流文件路径

[dcache]
offset-len = 4      # block offset 位数
index-len = 3       # set index 位数
log = mtrace_log.txt # 数据访存流文件路径
```

### 输出说明

程序会输出以下统计信息：

- **Total instructions/accesses**: 总指令数或访存次数
- **Cache hits**: Cache 命中次数
- **Cache misses**: Cache 缺失次数
- **Cache resets**: Cache 行失效次数（仅 dcache）
- **Hit rate**: 命中率
- **Miss rate**: 缺失率

示例输出：

```
=== Cache Simulation Results ===
Total instructions: 1000000
Cache hits: 950000
Cache misses: 50000
Hit rate: 95.00%
Miss rate: 5.00%
```

## 现代 C++ 特性应用

本项目使用了以下现代 C++17 特性：

### C++11 特性
- `auto` / `decltype`: 类型推导
- `nullptr`: 类型安全的空指针
- `override` / `final`: 虚函数控制
- `= default` / `= delete`: 特殊成员函数控制
- `std::unique_ptr`: 智能指针
- `std::move`: 移动语义
- `lambda`: 回调函数
- `[[nodiscard]]`: 属性标记

### C++17 特性
- `std::optional`: 可选值处理
- `if constexpr`: 编译期分支
- `std::string_view`: 字符串视图（部分使用）
- 结构化绑定：多返回值处理

### 代码风格
- **命名空间**：使用 `cachesim` 作为主命名空间
- **属性**：使用 `[[nodiscard]]` 标记不应忽略的返回值
- **异常安全**：使用 `noexcept` 标记不抛异常的函数
- **常量性**：优先使用 `const` 和 `constexpr`
- **RAII**：所有资源管理遵循 RAII 原则

## 设计亮点

1. **模板化的配置系统**：使用 C++ 模板实现类型安全的配置解析
2. **零开销抽象**：使用 `if constexpr` 实现编译期分支，无运行时开销
3. **现代 C++ 实践**：遵循现代 C++ 最佳实践，代码简洁高效
4. **模块化设计**：清晰的模块划分，易于扩展和维护

## 未来改进方向

- [ ] 支持更多的替换策略（如 LFU）
- [ ] 支持多级 cache 模拟
- [ ] 添加性能分析工具
- [ ] 支持批量参数扫描
- [ ] 添加单元测试框架

## 许可证

本项目为 ysyx 项目的一部分，遵循项目整体许可证。

## 贡献

欢迎提交 Issue 和 Pull Request！
